block (nop, inline) comment_todo {
    $(// TODO: fix bugs)
}
block (nop, inline) comment_todo2 {
    $(// TODO: implement dynamic configuration)
}
block (nop, inline) comment_todo3 {
    $(// TODO: refactor)
}
block (nop, inline) comment_todo4 {
    $(// TODO: simplify control flow)
}
block (nop, inline) comment_todo5 {
    $(// TODO: fail gracefully if we can)
}
block (nop, inline) comment_todo6 {
    $(// TODO: fix security issues)
}

block (nop, inline) comment_hack {
    $(// HACK: massive hack)
}
block (nop, inline) comment_hack2 {
    $(// HACK: ...don't look too closely here)
}
block (nop, inline) comment_hack3 {
    $(// HACK: magic!!!)
}
block (nop, inline) comment_hack4 {
    $(// HACK: this just *somehow* works)
}

block (nop, inline) comment_fixme {
    $(// FIXME)
}

block (nop, inline) comment_note {
    $(// NOTE: this isn't ideal)
}
block (nop, inline) comment_note2 {
    $(// NOTE: this shouldn't happen)
}
block (nop, inline) comment_note3 {
    $(// NOTE: bad practice, but it's neater than the alternative)
}
block (nop, inline) comment_note4 {
    $(// NOTE: welp. you're on your own now!)
}
