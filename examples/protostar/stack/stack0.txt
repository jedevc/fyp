// [compile]
// debug = true
// debug_separate = true
//
// [files]
// helpers.c
//
// [env]
// type = tcp-shell-setuid
// port = 4000

extern flag: fn () void,
       tryagain: fn () void

chunk buffer: [64]char,
      modified: int

block main {
    modified = 0
    gets@libc(buffer)
    if modified != 0 {
        flag()
    } else {
        tryagain()
    }
}
